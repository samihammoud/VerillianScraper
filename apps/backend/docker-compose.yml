services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: verillian
      POSTGRES_PASSWORD: verillian
      POSTGRES_DB: verillian
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
